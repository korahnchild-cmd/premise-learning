# 검수 리포트 반영 변경로그 (2026-07-22)

FABLE 5 전면검수 리포트 기반. "안전한 항목 전부" 범위로 반영.

## 반영 완료

### 데이터·콘텐츠
- **al-hist-003 정답 오류 수정**: `②`(오답) → `③ 신라→고구려→백제`(삼국사기 건국순서 신라 BC57·고구려 BC37·백제 BC18). 설명·오답노트 전면 정합화. (application-lab-data.js)
- **al-hist-006 교체**: 식민지 근대화론 논쟁 문항 → 조선총독부 통계 기반의 **사료 비판형**으로 교체. (application-lab-data.js)
- 문항 무결성 재검증: 200문항, 정답 정확히 1개씩, 중복 id·필수필드 누락 0.

### 신뢰·데이터 레이어 (common.js)
- **신규 방문자 가짜 이력 제거**: seed 기본값을 빈 계정으로. 데모 데이터는 `?demo=1` 파라미터일 때만 시딩.
- **저장형 XSS 차단**: `escapeHTML` 유틸 추가 → notebook·report의 questionLog 출력부 전부 이스케이프.
- **재플레이 중복 방지**: 이미 완료한 미션 재플레이 시 '발견한 전제'에 중복 기록 안 함.
- 이탈 방어용 `pauseSubscription`/`resumeSubscription` 추가(상태 `paused`).

### 게이팅 정합성
- **파이널 스파크 단건 판매 모순 해소**: 베이직에게 "프리미엄 전용입니다" 대신 **건별 19,900원 구매 경로** 노출. (final-spark.html)
- **course 심화 게이팅**: 베이직은 심화(Level3) 잠금, 프리미엄만 해제(`needsPremium`). 요금 카피와 일치. (course.html)
- **프리미엄 무료체험 경로 개통**: pricing 프리미엄 CTA → `onboarding.html?plan=premium`, startTrial에 plan 반영. (pricing/onboarding)
- **calendar**: 시험 카드의 '실전 적용 랩 진행 중' 블록·PREP_PLAN 랩 항목 삭제(랩=시험무관 원칙 준수). D-14 로드맵 '프리미엄에선' 문구로 수정.

### 정직성 카피
- **FAQ 허위 실적 문구 재작성**: "대부분의 학생이…", "많은 전문가들이…" → "저희가 설계한 목표 시간표" + 데이터 없음 명시. (faq.html)
- **"체험 끝나면 자동 결제" 수정**: → "결제 수단을 등록해야 이어져요"(카드 없는 체험과 정합). (mypage.html)
- **특허 출원번호 placeholder 숨김**: 실제 번호 확보 시까지 주석 처리. (index.html)
- Trust 카드 "현직 교사 감수" → "교사 감수 진행 예정". (index.html)
- FAQ 해지 위치 "대시보드" → "마이페이지".
- **이탈 방어**: mypage 해지 클릭 시 즉시 해지 대신 **일시정지 우선 제안** 단계 추가.

### 일관성
- 푸터 연령 표기 **"10~17세" → "초5~고2"** 전 14개 페이지 통일.
- 데모 인물명 **지우 → 지호** 통일.
- 누적 '발견한 전제' 수치 **58로 통일**(notebook 113 → 58, dashboard·report와 일치).
- 부모 화면 '전제 폭파' → **'발견한 전제'**로 이중화(notebook 캡션·툴팁, pricing 미리보기).
- 얼리버드 배너에 **"(결제 시 자동 적용)"** 명시. (index·pricing)
- index·pricing에 **"샘플 리포트 직접 보기 →"** 링크 추가.
- index 베이직 카드 '최상위권까지의 거리' 추가(3원 불일치 통일).

### 접근성·배포 위생
- 저대비 캡션색 `faint` 토큰 `#9AA4B2 → #6B7688` (WCAG AA 근접). 전 19개 config.
- daily 비구독자에 **"무료 체험판" 라벨** 추가(기대치 관리).
- course 데드코드(renderPreview/openPreviewModal) 삭제.

---

## 결정/추가 작업 필요 (미반영)

- **환불 불가 문구 법률 검토** — index FAQ·FAQ 환불 답변. 전자상거래법 청약철회(7일)·계속거래 중도해지 규정과 정합성 확인 필요. ※ 저는 변호사가 아니므로 결제 오픈 전 법률 검토 필수. (문구는 임의 수정하지 않음)
- **특허 출원번호 / 창업자 실명·사진** — 실제 값 확보 시 반영(현재 숨김).
- **"매일 새로운 사건"** — 고정 3케이스 → 최소 요일별 로테이션(런칭 조건, 별도 작업).
- **reduced-motion 장식 루프** — !important 강제 재생 완화(WCAG 2.3.3). 페이지별 keyframe 손질 필요, 별도 작업.
- **stale 파일 git rm** — `.restored` 7종 + `kozy-icons.html` (sandbox에서 git 불가 → 아래 명령어로).
- 성능: `cdn.tailwindcss.com` 런타임 → 빌드 산출물, 대용량 PNG WebP 변환(런칭 전).

## 배포 명령어
```
cd "D:\AI_인공지능\앱비지니스\전제학습\site"
git rm academy-guide.html.restored course.html.restored demo.html.restored faq.html.restored index.html.restored onboarding.html.restored pricing.html.restored kozy-icons.html
git add -A
git commit -m "검수 반영: 데이터 오류·게이팅 정합성·정직성 카피·접근성 수정"
git push
```
